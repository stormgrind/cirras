$: << 'lib/boxgrinder-build/lib'
$: << 'lib/cantiere/lib'

require 'boxgrinder/helpers/rake-helper'
require 'cantiere/helpers/rake-helper'

Cantiere::RakeHelper.new( BoxGrinder::RakeHelper.new( :name => "CirrAS", :version => "1.0.0.Beta1", :release => "" ).config )
